name: Backend

on:
  push:
    branches:
      - feature/backend/ci
  workflow_dispatch:

env:
  DB_DATABASE: ${{ secrets.DB_DATABASE }}
  DB_USERNAME: ${{ secrets.DB_USERNAME }}
  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

jobs:
  build:
    name: 📦 Laravel Build
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout репозитория
        uses: actions/checkout@v4

      - name: 🔐 Docker Login to GHCR
        run: echo "${{ secrets.CR_PAT }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: 📝 Создать .env и записать секреты
        working-directory: backend
        run: |
          cp .env.example .env
          echo "DB_DATABASE=${DB_DATABASE}" >> .env
          echo "DB_USERNAME=${DB_USERNAME}" >> .env
          echo "DB_PASSWORD=${DB_PASSWORD}" >> .env

      - name: 🧑‍💻 Export runner UID/GID
        run: |
          echo "UID=$(id -u)" >> $GITHUB_ENV
          echo "GID=$(id -g)" >> $GITHUB_ENV

      - name: 🔧 Сборка Docker-контейнеров
        working-directory: deployment
        run: |
          docker compose build \
            --build-arg UID=$UID \
            --build-arg GID=$GID

      - name: 🚀 Laravel build внутри контейнера
        working-directory: deployment
        run: |
          docker compose up -d
          docker compose run --rm composer composer install --no-dev --optimize-autoloader
          docker compose run --rm composer php artisan config:cache
          docker compose run --rm composer php artisan route:cache
          docker compose run --rm composer php artisan view:cache

      - name: 🐳 Посмотреть образы после сборки
        run: docker image ls

      - name: 📦 Публикация PHP-образа в GHCR
        working-directory: deployment
        run: |
          OWNER_LC=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          PHP_IMAGE="ghcr.io/${OWNER_LC}/deployment-php"

          docker tag deployment-php $PHP_IMAGE:latest
          docker tag deployment-php $PHP_IMAGE:${{ github.sha }}

          docker push $PHP_IMAGE:latest
          docker push $PHP_IMAGE:${{ github.sha }}

  deployment:
    name: Laravel Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Setup SSH and test connection
        run: |
          echo "🔐 Настройка SSH ключа..."
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519

          echo "📥 Добавление удалённого хоста в known_hosts..."
          ssh-keyscan -H "${{ secrets.REMOTE_HOST }}" >> ~/.ssh/known_hosts

          echo "🚀 Проверка подключения к ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }}"
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_ed25519 \
              ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }} \
              "echo '✅ SSH соединение установлено'"